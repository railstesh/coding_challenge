<tr>
  <td><%= tool.id %></td>
  <td><%= tool.name %></td>
  <td><%= tool.language %></td>
  <td><%= tool.json_spec.to_s.truncate(50) %></td>
  <td><%= link_to 'Show', tool %></td>
  <td><%= link_to 'Edit', edit_tool_path(tool) %></td>
  <td><%= link_to 'Destroy', tool, method: :delete, data: { confirm: 'Are you sure?' } %>
  <td><%= link_to 'Update Translation', update_translation_tool_path(tool) %></td>
</tr>
